- name: install nginx
  hosts: frontend
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.service:
        name: nginx
        state: present

    - name: start nginx service
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: true

   